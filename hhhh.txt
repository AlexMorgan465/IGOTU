Private Sub CommandButton_Move_Click()
    Dim fso As Object
    Dim source As String
    Dim destination As String
    
    ' Vérifier sélection
    If TreeView1.SelectedItem Is Nothing Then
        MsgBox "Sélectionne un dossier source", vbExclamation
        Exit Sub
    End If
    
    source = TreeView1.SelectedItem.Key
    
    ' Ouvrir fenêtre parcourir
    With Application.FileDialog(msoFileDialogFolderPicker)
        .Title = "Choisir le dossier de destination"
        If .Show <> -1 Then Exit Sub
        destination = .SelectedItems(1)
    End With
    
    Set fso = CreateObject("Scripting.FileSystemObject")
    
    ' Déplacer
    fso.MoveFolder source, destination
    
    MsgBox "Dossier déplacé avec succès", vbInformation
    
    ' Refresh
    TreeView1.Nodes.Clear
    Call ChargerDossier("C:\Test", Nothing)
End Sub
Private Sub CommandButton_Copy_Click()
    Dim fso As Object
    Dim source As String
    Dim destination As String
    
    ' Vérifier sélection
    If TreeView1.SelectedItem Is Nothing Then
        MsgBox "Sélectionne un dossier source", vbExclamation
        Exit Sub
    End If
    
    source = TreeView1.SelectedItem.Key
    
    ' Ouvrir fenêtre parcourir
    With Application.FileDialog(msoFileDialogFolderPicker)
        .Title = "Choisir le dossier de destination"
        If .Show <> -1 Then Exit Sub
        destination = .SelectedItems(1)
    End With
    
    Set fso = CreateObject("Scripting.FileSystemObject")
    
    ' Copier
    fso.CopyFolder source, destination
    
    MsgBox "Dossier copié avec succès", vbInformation
End Sub
