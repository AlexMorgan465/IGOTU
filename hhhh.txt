<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cofidis - Expert Tour de France</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Montserrat', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #1a1a1a 0%, #333333 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            position: relative;
            overflow: hidden;
        }

        /* Effet de route de vélo en arrière-plan */
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: repeating-linear-gradient(45deg, rgba(255,215,0,0.02) 0px, rgba(255,215,0,0.02) 20px, rgba(237,28,36,0.02) 20px, rgba(237,28,36,0.02) 40px);
            pointer-events: none;
        }

        .chatbot-container {
            width: 100%;
            max-width: 600px;
            background: white;
            border-radius: 30px;
            box-shadow: 0 30px 60px rgba(237, 28, 36, 0.3);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            height: 80vh;
            max-height: 800px;
            border: 3px solid #FFD700;
            position: relative;
            z-index: 1;
        }

        /* Header Cofidis Team - Rouge et Jaune */
        .chatbot-header {
            background: linear-gradient(135deg, #ED1C24 0%, #B22222 100%);
            color: white;
            padding: 25px 20px;
            text-align: center;
            position: relative;
            overflow: hidden;
            border-bottom: 4px solid #FFD700;
        }

        /* Effet de maillot jaune */
        .chatbot-header::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 300px;
            height: 300px;
            background: rgba(255, 215, 0, 0.15);
            border-radius: 50%;
            transform: rotate(45deg);
        }

        /* Effet de vélo */
        .chatbot-header::after {
            content: '🚴';
            position: absolute;
            bottom: -10px;
            left: 20px;
            font-size: 60px;
            opacity: 0.1;
            transform: rotate(15deg);
        }

        .cofidis-logo {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            margin-bottom: 10px;
            position: relative;
            z-index: 1;
        }

        .logo-text {
            font-size: 2.5rem;
            font-weight: 800;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .logo-text span:first-child {
            color: #FFD700;
            text-shadow: 2px 2px 0 #B22222;
        }

        .logo-text span:last-child {
            color: white;
            text-shadow: 2px 2px 0 #FFD700;
        }

        .tour-de-france {
            background: #FFD700;
            color: #ED1C24;
            padding: 5px 15px;
            border-radius: 30px;
            font-weight: 700;
            font-size: 0.9rem;
            display: inline-block;
            margin-top: 5px;
            box-shadow: 0 4px 0 #B22222;
        }

        .chatbot-header h1 {
            font-size: 1.5rem;
            font-weight: 600;
            margin: 10px 0 5px;
            position: relative;
            z-index: 1;
        }

        .chatbot-header p {
            font-size: 0.9rem;
            opacity: 0.95;
            position: relative;
            z-index: 1;
            font-weight: 400;
            background: rgba(0,0,0,0.2);
            padding: 5px 15px;
            border-radius: 30px;
            display: inline-block;
        }

        /* Messages Area */
        .chat-messages {
            flex: 1;
            overflow-y: auto;
            padding: 20px;
            background: #F5F5F5;
        }

        .message {
            margin-bottom: 20px;
            display: flex;
            flex-direction: column;
        }

        .user-message {
            align-items: flex-end;
        }

        .bot-message {
            align-items: flex-start;
        }

        .message-content {
            max-width: 80%;
            padding: 14px 18px;
            border-radius: 18px;
            font-size: 0.95rem;
            line-height: 1.5;
            word-wrap: break-word;
            box-shadow: 0 3px 10px rgba(237, 28, 36, 0.2);
        }

        .user-message .message-content {
            background: #ED1C24;
            color: white;
            border-bottom-right-radius: 4px;
            border: 2px solid #FFD700;
        }

        .bot-message .message-content {
            background: white;
            color: #2D3748;
            border-bottom-left-radius: 4px;
            border-left: 4px solid #FFD700;
            border-right: 2px solid #ED1C24;
        }

        .timestamp {
            font-size: 0.7rem;
            color: #666;
            margin-top: 5px;
            margin-bottom: 5px;
            font-weight: 500;
        }

        .user-message .timestamp {
            text-align: right;
            color: #ED1C24;
        }

        .bot-message .timestamp {
            color: #B8860B;
        }

        /* Typing Indicator */
        .typing-indicator {
            display: flex;
            padding: 14px 18px;
            background: white;
            border-radius: 18px;
            border-bottom-left-radius: 4px;
            border-left: 4px solid #FFD700;
            border-right: 2px solid #ED1C24;
            width: fit-content;
        }

        .typing-indicator span {
            height: 10px;
            width: 10px;
            background: #ED1C24;
            border-radius: 50%;
            display: inline-block;
            margin: 0 3px;
            animation: typing 1.4s infinite ease-in-out;
        }

        .typing-indicator span:nth-child(2) {
            animation-delay: 0.2s;
            background: #FFD700;
        }

        .typing-indicator span:nth-child(3) {
            animation-delay: 0.4s;
            background: #ED1C24;
        }

        @keyframes typing {
            0%, 60%, 100% { transform: translateY(0); opacity: 0.6; }
            30% { transform: translateY(-8px); opacity: 1; }
        }

        /* Input Area */
        .chat-input-container {
            display: flex;
            padding: 20px;
            background: white;
            border-top: 3px solid #FFD700;
            position: relative;
        }

        #userInput {
            flex: 1;
            padding: 14px 18px;
            border: 2px solid #ED1C24;
            border-radius: 30px;
            font-size: 1rem;
            outline: none;
            transition: all 0.3s;
            color: #2D3748;
        }

        #userInput:focus {
            border-color: #FFD700;
            box-shadow: 0 0 0 4px rgba(255, 215, 0, 0.3);
        }

        #userInput::placeholder {
            color: #999;
            font-weight: 400;
        }

        #sendButton {
            background: #ED1C24;
            color: white;
            border: none;
            width: 55px;
            height: 55px;
            border-radius: 50%;
            margin-left: 10px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
            box-shadow: 0 4px 10px rgba(237, 28, 36, 0.4);
            border: 2px solid #FFD700;
        }

        #sendButton:hover {
            background: #B22222;
            transform: scale(1.1) rotate(10deg);
            box-shadow: 0 6px 15px rgba(255, 215, 0, 0.5);
        }

        #sendButton:disabled {
            background: #999;
            border-color: #666;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        #sendButton svg {
            width: 22px;
            height: 22px;
        }

        /* Suggested Questions - Cofidis Team Style */
        .suggested-questions {
            padding: 15px 20px;
            background: #F5F5F5;
            border-top: 2px dashed #FFD700;
        }

        .suggested-title {
            font-size: 0.9rem;
            color: #ED1C24;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 800;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .suggested-title::before {
            content: '🚴';
            font-size: 1.2rem;
        }

        .suggested-title::after {
            content: '';
            flex: 1;
            height: 3px;
            background: linear-gradient(90deg, #ED1C24, #FFD700, #ED1C24);
        }

        .suggested-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }

        .suggested-chip {
            background: white;
            border: 2px solid #ED1C24;
            border-radius: 30px;
            padding: 12px 18px;
            font-size: 0.85rem;
            cursor: pointer;
            transition: all 0.3s;
            color: #ED1C24;
            font-weight: 600;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .suggested-chip:hover {
            background: #ED1C24;
            color: white;
            border-color: #FFD700;
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(237, 28, 36, 0.4);
        }

        /* Maillot jaune pour le meilleur message */
        .message.bot-message:first-child .message-content {
            border-left-width: 6px;
            border-left-color: #FFD700;
            background: #FFF9E6;
            position: relative;
        }

        .message.bot-message:first-child .message-content::after {
            content: '🏆';
            position: absolute;
            top: -10px;
            right: -10px;
            font-size: 20px;
            background: #FFD700;
            border-radius: 50%;
            padding: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }

        /* Scrollbar personnalisée */
        .chat-messages::-webkit-scrollbar {
            width: 8px;
        }

        .chat-messages::-webkit-scrollbar-track {
            background: #F5F5F5;
        }

        .chat-messages::-webkit-scrollbar-thumb {
            background: #ED1C24;
            border-radius: 4px;
            border: 2px solid #FFD700;
        }

        .chat-messages::-webkit-scrollbar-thumb:hover {
            background: #B22222;
        }
    </style>
</head>
<body>
    <div class="chatbot-container">
        <div class="chatbot-header">
            <div class="cofidis-logo">
                <div class="logo-text">
                    <span>COF</span><span>IDIS</span>
                </div>
            </div>
            <div class="tour-de-france">
                ★ SPONSOR OFFICIEL TOUR DE FRANCE ★
            </div>
            <h1>Expert 10 Questions</h1>
            <p>Équipe de France du Crédit</p>
        </div>

        <div class="chat-messages" id="chatMessages">
            <div class="message bot-message">
                <div class="message-content">
                    🚴‍♂️ Bonjour et bienvenue chez Cofidis, partenaire officiel du Tour de France !<br><br>
                    Je suis spécialisé dans 10 questions spécifiques. Posez-moi vos questions sur les PSM, décomptes, échéances, assurances, etc. Je reconnais vos questions même si elles sont formulées différemment !
                </div>
                <div class="timestamp">Départ - 0 km</div>
            </div>
        </div>

        <div class="suggested-questions">
            <div class="suggested-title">QUESTIONS SPRINT</div>
            <div class="suggested-grid" id="suggestedGrid">
                <span class="suggested-chip" onclick="useSuggestion('Qu\'est-ce qu\'un PSM?')">🚵 PSM</span>
                <span class="suggested-chip" onclick="useSuggestion('Comment trouver l\'échéance du mois?')">📅 Échéance</span>
                <span class="suggested-chip" onclick="useSuggestion('C\'est quoi le taux usuraire?')">📊 Taux usuraire</span>
                <span class="suggested-chip" onclick="useSuggestion('Que signifie OCI?')">📁 OCI</span>
                <span class="suggested-chip" onclick="useSuggestion('Comment gérer un client sous tutelle?')">👤 Tutelle</span>
                <span class="suggested-chip" onclick="useSuggestion('Qu\'est-ce que la FIPEN?')">📄 FIPEN</span>
                <span class="suggested-chip" onclick="useSuggestion('Rebond assurance?')">🛡️ Assurance</span>
                <span class="suggested-chip" onclick="useSuggestion('Date décompte?')">📆 Décompte</span>
            </div>
        </div>

        <div class="chat-input-container">
            <input type="text" id="userInput" placeholder="Posez votre question..." onkeypress="handleKeyPress(event)">
            <button id="sendButton" onclick="sendMessage()">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="22" y1="2" x2="11" y2="13"></line>
                    <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
                </svg>
            </button>
        </div>
    </div>

    <script>
        // Base de connaissances avec mots-clés et réponses (identique)
        const knowledgeBase = [
            {
                id: 1,
                keywords: ['psm', 'prêt sur mesure', 'pret sur mesure', 'crédit amortissable', '500', '35000', '6 mois', '84 mois'],
                response: "🚵 Le Prêt sur Mesure (PSM) est un crédit amortissable. Il peut être ouvert pour un montant de 500€ jusqu'à 35 000€. Selon le montant emprunté, la durée varie de 6 à 84 mois."
            },
            {
                id: 2,
                keywords: ['décompte', 'date décompte', 'positionner décompte', 'envoyer décompte', 'décompte 7', 'consultation décompte'],
                response: "📆 Le décompte est à consulter à la date de la prochaine échéance. Exemple: nous sommes le 7/02, l'échéance du contrat est au 5, vous consultez le décompte en date du 5/03."
            },
            {
                id: 3,
                keywords: ['échéance du mois', 'trouver échéance', 'voir échéance', 'gec échéance', 'événements échéance'],
                response: "📅 L'échéance du mois d'un contrat est visible dans GEC, Onglet [Evénements] vue 'Opérations' sur la ligne 'Echéance'. Cette ligne est tracée en date de quantième du contrat."
            },
            {
                id: 4,
                keywords: ['rebond assurance', 'assurance non souscrite', 'incomplet assurance', 'accessio', 'prêt rénovation', 'conseiller assurance'],
                response: "🛡️ Dans le cadre d'une étude crédit sur Accessio, PSM, PSM sur PSM, prêt rénovation, si le tiers n'a pas souscrit à l'assurance et que le dossier est complet, le conseiller positionne manuellement un incomplet assurance pour demander au client de nous recontacter afin de lui conseiller l'assurance sur son crédit."
            },
            {
                id: 5,
                keywords: ['modifier date prélèvement', 'date prélèvement 4cb', '4cb', '3cb', 'pnf', 'modifier échéance monétique', 'date échéance modifiable'],
                response: "💳 Les dates d'échéances ne sont pas modifiables sur les produits monétiques (3CB, 4CB ou PNF 3/4)."
            },
            {
                id: 6,
                keywords: ['client sous tutelle', 'tutelle', 'curatelle', 'affaires spécifiques', 'gestion tutelle', 'secteur affaires spécifiques'],
                response: "👥 Un client sous tutelle est géré par le Secteur des Affaires Spécifiques."
            },
            {
                id: 7,
                keywords: ['fipen', 'fiche précontractuelle', 'information européenne', 'loi lagarde', 'offre crédit consommation'],
                response: "📋 La FIPEN est la fiche précontractuelle d'information européenne normalisée, rendue obligatoire par la loi Lagarde dans le cadre de la signature d'une offre de crédit à la consommation."
            },
            {
                id: 8,
                keywords: ['taux usuraire', 'usure', 'taux d\'usure', 'trimestre usure', 'janvier avril juillet octobre'],
                response: "📊 Le taux d'usure est revu tous les trimestres : janvier / avril / juillet / octobre. Celui-ci diffère selon les produits."
            },
            {
                id: 9,
                keywords: ['oci', 'dossier oci', 'incomplet oci', 'se+ oci', 'signification oci', 'géré en incomplet'],
                response: "📁 Un dossier en OCI peut avoir 2 significations : soit le dossier est géré en incomplet par le service acceptation, soit pour le process SE+ c'est un état qui est poussé avec un sous motif afin que le conseiller puisse le traiter en fonction des attendus liés au sous motif."
            },
            {
                id: 10,
                keywords: ['cpp', 'justifier cpp', 'répondre client cpp', 'nouvelle proposition', 'mise à jour données'],
                response: "⚡ Il faut expliquer au client que suite à la mise à jour de l'ensemble des données de son dossier nous sommes amenés à faire cette nouvelle proposition adaptée."
            }
        ];

        let isTyping = false;

        function handleKeyPress(event) {
            if (event.key === 'Enter' && !isTyping) {
                sendMessage();
            }
        }

        function useSuggestion(question) {
            document.getElementById('userInput').value = question;
            sendMessage();
        }

        function findBestMatch(userQuestion) {
            const questionLower = userQuestion.toLowerCase();
            
            const words = questionLower.split(' ');
            
            let bestMatch = null;
            let highestScore = 0;

            for (const item of knowledgeBase) {
                let score = 0;
                
                for (const keyword of item.keywords) {
                    const keywordLower = keyword.toLowerCase();
                    
                    if (questionLower.includes(keywordLower)) {
                        score += 3;
                    }
                    
                    for (const word of words) {
                        if (word.length > 2) {
                            if (keywordLower.includes(word) || word.includes(keywordLower)) {
                                score += 1;
                            }
                        }
                    }
                }
                
                if (score > highestScore) {
                    highestScore = score;
                    bestMatch = item;
                }
            }

            return bestMatch;
        }

        function addMessage(text, isUser = false) {
            const messagesContainer = document.getElementById('chatMessages');
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${isUser ? 'user-message' : 'bot-message'}`;
            
            const contentDiv = document.createElement('div');
            contentDiv.className = 'message-content';
            contentDiv.textContent = text;
            
            const timestampDiv = document.createElement('div');
            timestampDiv.className = 'timestamp';
            const now = new Date();
            
            // Thème Tour de France pour les timestamps
            if (isUser) {
                timestampDiv.textContent = `${now.toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit' })} - ⚡ Sprint`;
            } else {
                const km = Math.floor(Math.random() * 100) + 1;
                timestampDiv.textContent = `${now.toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit' })} - ${km} km`;
            }
            
            messageDiv.appendChild(contentDiv);
            messageDiv.appendChild(timestampDiv);
            messagesContainer.appendChild(messageDiv);
            
            messagesContainer.scrollTop = messagesContainer.scrollHeight;
        }

        function showTypingIndicator() {
            const messagesContainer = document.getElementById('chatMessages');
            const typingDiv = document.createElement('div');
            typingDiv.className = 'message bot-message';
            typingDiv.id = 'typingIndicator';
            
            const indicatorDiv = document.createElement('div');
            indicatorDiv.className = 'typing-indicator';
            indicatorDiv.innerHTML = '<span></span><span></span><span></span>';
            
            typingDiv.appendChild(indicatorDiv);
            messagesContainer.appendChild(typingDiv);
            messagesContainer.scrollTop = messagesContainer.scrollHeight;
        }

        function removeTypingIndicator() {
            const typingIndicator = document.getElementById('typingIndicator');
            if (typingIndicator) {
                typingIndicator.remove();
            }
        }

        function sendMessage() {
            const input = document.getElementById('userInput');
            const question = input.value.trim();
            
            if (question === '' || isTyping) return;
            
            addMessage(question, true);
            input.value = '';
            
            isTyping = true;
            document.getElementById('userInput').disabled = true;
            document.getElementById('sendButton').disabled = true;
            
            showTypingIndicator();
            
            setTimeout(() => {
                removeTypingIndicator();
                
                const match = findBestMatch(question);
                
                if (match) {
                    addMessage(match.response);
                } else {
                    addMessage("🏁 Désolé, je ne peux répondre qu'aux 10 questions du parcours. Les thèmes disponibles sont : PSM, décomptes, échéances, rebond assurance, modifications de dates, tutelle, FIPEN, taux usuraire, OCI, et justifications CPP.");
                }
                
                isTyping = false;
                document.getElementById('userInput').disabled = false;
                document.getElementById('sendButton').disabled = false;
                document.getElementById('userInput').focus();
            }, 800);
        }

        window.onload = function() {
            document.getElementById('userInput').focus();
        };
    </script>
</body>
</html>
